@layout('layouts.main')

@section('content')
<div class="container">
  @each(post in chenjings)
    <h3 class="mt-5 mb-4">{{post.title}}</h3>
    <div>{{post.content}}</div>
  @endeach
</div>
@endsection
